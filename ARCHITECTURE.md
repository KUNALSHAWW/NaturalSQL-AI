# 🏗️ System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                      (Streamlit Web App)                        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Chat UI    │  │ Config Panel │  │ Query History│        │
│  │              │  │              │  │              │        │
│  │ • Messages   │  │ • Model      │  │ • Timestamps │        │
│  │ • Input Box  │  │ • Temp       │  │ • Past Qs    │        │
│  │ • Streaming  │  │ • Max Iter   │  │ • Schema     │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
└─────────┼──────────────────┼──────────────────┼────────────────┘
          │                  │                  │
          └──────────────────┴──────────────────┘
                             │
          ┌──────────────────▼────────────────────┐
          │      APPLICATION LOGIC LAYER          │
          │         (Python Backend)              │
          │                                       │
          │  ┌─────────────────────────────────┐ │
          │  │   LangChain Agent Orchestrator  │ │
          │  │                                 │ │
          │  │  • Zero-Shot React Agent       │ │
          │  │  • SQL Database Toolkit        │ │
          │  │  • Query Planning & Execution  │ │
          │  │  • Error Recovery              │ │
          │  └─────────────┬───────────────────┘ │
          └────────────────┼─────────────────────┘
                           │
          ┌────────────────┼─────────────────────┐
          │                ▼                     │
          │    ┌──────────────────────┐         │
          │    │   Groq LLM API       │         │
          │    │                      │         │
          │    │ • Llama3-8b-8192    │         │
          │    │ • Llama3-70b-8192   │         │
          │    │ • Mixtral-8x7b      │         │
          │    └──────────┬───────────┘         │
          └───────────────┼─────────────────────┘
                          │
          ┌───────────────▼──────────────────────┐
          │      DATABASE ABSTRACTION LAYER      │
          │         (SQLAlchemy ORM)             │
          │                                      │
          │  ┌────────────┐   ┌──────────────┐  │
          │  │ Connection │   │ Query Engine │  │
          │  │   Pool     │   │              │  │
          │  │            │   │ • Parser     │  │
          │  │ • Caching  │   │ • Executor   │  │
          │  │ • Retry    │   │ • Validator  │  │
          │  └────────────┘   └──────────────┘  │
          └───────────────┬──────────────────────┘
                          │
          ┌───────────────▼──────────────────────┐
          │         DATA STORAGE LAYER           │
          │                                      │
          │  ┌──────────┐        ┌───────────┐  │
          │  │  SQLite  │        │   MySQL   │  │
          │  │          │        │           │  │
          │  │ • Local  │        │ • Remote  │  │
          │  │ • Fast   │        │ • Prod    │  │
          │  └──────────┘        └───────────┘  │
          └──────────────────────────────────────┘
```

## Data Flow

```
┌──────────┐
│   USER   │
└────┬─────┘
     │
     │ 1. Natural Language Query
     │    "Show students with marks > 85"
     ▼
┌─────────────────┐
│  STREAMLIT UI   │
│                 │
│ • Capture input │
│ • Display msgs  │
└────┬────────────┘
     │
     │ 2. Send to Agent
     ▼
┌──────────────────┐
│ LANGCHAIN AGENT  │
│                  │
│ • Parse intent   │
│ • Plan query     │
└────┬─────────────┘
     │
     │ 3. Request LLM reasoning
     ▼
┌──────────────┐
│  GROQ LLM    │
│              │
│ • Generate   │
│   SQL query  │
└────┬─────────┘
     │
     │ 4. SQL: "SELECT * FROM STUDENT WHERE MARKS > 85"
     ▼
┌──────────────────┐
│  SQL TOOLKIT     │
│                  │
│ • Validate query │
│ • Execute safely │
└────┬─────────────┘
     │
     │ 5. Execute query
     ▼
┌──────────────┐
│   DATABASE   │
│              │
│ • Run query  │
│ • Return     │
│   results    │
└────┬─────────┘
     │
     │ 6. Raw data
     ▼
┌──────────────────┐
│  SQL TOOLKIT     │
│                  │
│ • Format results │
└────┬─────────────┘
     │
     │ 7. Formatted data
     ▼
┌──────────────────┐
│ LANGCHAIN AGENT  │
│                  │
│ • Process        │
│ • Add context    │
└────┬─────────────┘
     │
     │ 8. Natural language response
     ▼
┌─────────────────┐
│  STREAMLIT UI   │
│                 │
│ • Display       │
│ • Stream output │
└────┬────────────┘
     │
     │ 9. Final Answer
     ▼
┌──────────┐
│   USER   │
└──────────┘
```

## Component Interactions

```
┌─────────────────────────────────────────────────┐
│              SESSION STATE                      │
│                                                 │
│  • messages: List[Dict]                        │
│  • query_history: List[Dict]                   │
│  • show_schema: bool                           │
│  • bookmarks: List[str]                        │
└────────────────┬────────────────────────────────┘
                 │
                 │ Manages state across requests
                 ▼
┌────────────────────────────────────────────────┐
│         STREAMLIT APPLICATION                  │
│                                                │
│  ┌──────────────┐         ┌────────────────┐  │
│  │   Sidebar    │◄───────►│   Main Area    │  │
│  │              │         │                │  │
│  │ • DB Config  │         │ • Chat         │  │
│  │ • API Key    │         │ • Results      │  │
│  │ • Settings   │         │ • History      │  │
│  └──────────────┘         └────────────────┘  │
└────────────────┬───────────────────────────────┘
                 │
                 │ Configuration passed to agent
                 ▼
┌────────────────────────────────────────────────┐
│          LANGCHAIN COMPONENTS                  │
│                                                │
│  ┌────────────┐     ┌──────────────────────┐  │
│  │    LLM     │────►│   SQL Toolkit        │  │
│  │   (Groq)   │     │                      │  │
│  │            │     │ • list_tables()      │  │
│  │ • Llama3   │     │ • describe_tables()  │  │
│  │ • Mixtral  │     │ • query_checker()    │  │
│  └────────────┘     │ • query_executor()   │  │
│         │           └──────────────────────┘  │
│         │                      │              │
│         └──────────┬───────────┘              │
│                    ▼                          │
│         ┌─────────────────────┐               │
│         │   SQL Agent         │               │
│         │                     │               │
│         │ • Reasoning Loop    │               │
│         │ • Action Selection  │               │
│         │ • Error Handling    │               │
│         └─────────────────────┘               │
└────────────────┬───────────────────────────────┘
                 │
                 │ Query execution
                 ▼
┌────────────────────────────────────────────────┐
│            DATABASE LAYER                      │
│                                                │
│  ┌──────────────────┐   ┌──────────────────┐  │
│  │  SQLAlchemy      │   │  Connection Pool │  │
│  │  Engine          │◄──┤                  │  │
│  │                  │   │ • Max: 10        │  │
│  │ • execute()      │   │ • Timeout: 30s   │  │
│  │ • fetch()        │   │ • Retry: 3       │  │
│  │ • transaction()  │   └──────────────────┘  │
│  └────────┬─────────┘                         │
└───────────┼───────────────────────────────────┘
            │
            │ Database operations
            ▼
   ┌────────────────┐
   │   SQLite /     │
   │   MySQL        │
   │                │
   │ • Tables       │
   │ • Indexes      │
   │ • Constraints  │
   └────────────────┘
```

## Security Architecture

```
┌──────────────────────────────────────────┐
│           SECURITY LAYERS                │
│                                          │
│  1. INPUT VALIDATION                     │
│     ┌────────────────────────────┐      │
│     │ • API Key Masking          │      │
│     │ • Password Type: "password"│      │
│     │ • Input Sanitization       │      │
│     └────────────────────────────┘      │
│                                          │
│  2. DATABASE SECURITY                    │
│     ┌────────────────────────────┐      │
│     │ • Read-Only Mode (SQLite)  │      │
│     │ • Parameterized Queries    │      │
│     │ • Connection Pooling       │      │
│     │ • Timeout Limits           │      │
│     └────────────────────────────┘      │
│                                          │
│  3. API SECURITY                         │
│     ┌────────────────────────────┐      │
│     │ • API Key Storage (Memory) │      │
│     │ • No Key in Logs           │      │
│     │ • HTTPS Communication      │      │
│     └────────────────────────────┘      │
│                                          │
│  4. ERROR HANDLING                       │
│     ┌────────────────────────────┐      │
│     │ • Try-Catch Blocks         │      │
│     │ • User-Friendly Messages   │      │
│     │ • No Stack Trace Exposure  │      │
│     └────────────────────────────┘      │
└──────────────────────────────────────────┘
```

## Caching Strategy

```
┌──────────────────────────────────────┐
│         CACHING LAYERS               │
│                                      │
│  1. STREAMLIT CACHE                  │
│     ┌──────────────────────┐        │
│     │ @st.cache_resource   │        │
│     │                      │        │
│     │ • DB Connections     │        │
│     │ • TTL: 2 hours       │        │
│     │ • Shared across      │        │
│     │   sessions           │        │
│     └──────────────────────┘        │
│                                      │
│  2. SESSION STATE                    │
│     ┌──────────────────────┐        │
│     │ st.session_state     │        │
│     │                      │        │
│     │ • Messages           │        │
│     │ • Query History      │        │
│     │ • User Preferences   │        │
│     │ • Per-session        │        │
│     └──────────────────────┘        │
│                                      │
│  3. CONNECTION POOLING               │
│     ┌──────────────────────┐        │
│     │ SQLAlchemy Pool      │        │
│     │                      │        │
│     │ • Pool Size: 5       │        │
│     │ • Max Overflow: 10   │        │
│     │ • Recycle: 3600s     │        │
│     └──────────────────────┘        │
└──────────────────────────────────────┘
```

## Error Recovery Flow

```
┌─────────────┐
│ User Query  │
└──────┬──────┘
       │
       ▼
┌──────────────┐
│ Validation   │
│ Check        │
└──────┬───────┘
       │
       │ Valid?
       ├─No──► ┌──────────────┐
       │       │ Show Error   │
       │       │ Suggest Fix  │
       │       └──────────────┘
       │
       Yes
       │
       ▼
┌──────────────┐
│ Execute      │
│ Query        │
└──────┬───────┘
       │
       │ Success?
       ├─No──► ┌──────────────────┐
       │       │ Parsing Error?   │
       │       └────┬─────────────┘
       │            │
       │            Yes
       │            │
       │            ▼
       │       ┌──────────────────┐
       │       │ Retry with       │
       │       │ Error Handler    │
       │       └────┬─────────────┘
       │            │
       │            │ Success?
       │            ├─No──► ┌─────────────┐
       │            │       │ Final Error │
       │            │       │ Message     │
       │            │       └─────────────┘
       │            Yes
       │            │
       Yes          │
       │            │
       └────────┬───┘
                │
                ▼
         ┌──────────────┐
         │ Format and   │
         │ Display      │
         │ Results      │
         └──────────────┘
```

## Technology Stack Layers

```
┌─────────────────────────────────────────────┐
│         PRESENTATION LAYER                  │
│                                             │
│  Streamlit v1.28+                          │
│  • HTML/CSS (Custom)                       │
│  • JavaScript (Built-in)                   │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│         APPLICATION LAYER                   │
│                                             │
│  Python 3.8+                               │
│  • LangChain Framework                     │
│  • Custom Business Logic                   │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│         INTEGRATION LAYER                   │
│                                             │
│  • Groq API Client                         │
│  • SQLAlchemy ORM                          │
│  • Database Connectors                     │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│         EXTERNAL SERVICES                   │
│                                             │
│  ┌──────────────┐    ┌─────────────────┐   │
│  │  Groq Cloud  │    │   Databases     │   │
│  │              │    │                 │   │
│  │  • Llama3    │    │  • SQLite       │   │
│  │  • Mixtral   │    │  • MySQL        │   │
│  └──────────────┘    └─────────────────┘   │
└─────────────────────────────────────────────┘
```

---

This architecture enables:
✅ **Scalability** - Modular design allows easy expansion
✅ **Maintainability** - Clear separation of concerns
✅ **Security** - Multiple layers of protection
✅ **Performance** - Caching and connection pooling
✅ **Reliability** - Comprehensive error handling
